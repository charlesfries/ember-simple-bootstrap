import BsBaseComponent from './base';

/**
 * ARGS
 * flush?: boolean;
 * numbered?: boolean;
 * horizontal?: boolean;
 * color?: string;
 */

export default class BsListGroupComponent extends BsBaseComponent {}
