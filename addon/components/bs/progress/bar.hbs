<div
  class="progress-bar
    {{if @color (concat 'bg-' @color)}}
    {{if @striped 'progress-bar-striped'}}
    {{if @animated 'progress-bar-animated'}}"
  role="progressbar"
  style={{html-safe (concat "width:" @value "%")}}
  ...attributes
>
  {{#if @labeled}}
    {{@value}}%
  {{/if}}
</div>