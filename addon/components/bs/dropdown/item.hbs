<button
	type="button"
	class="dropdown-item {{if @color (concat "text-" @color)}}"
	disabled={{@disabled}}
	{{on "click" (optional @onClick)}}
	...attributes
>
	{{#if (has-block)}}
		{{yield}}
	{{else}}
		{{@label}}
	{{/if}}
</button>